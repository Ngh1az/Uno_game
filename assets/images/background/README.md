# Ảnh nền (tùy chọn)

Đặt file PNG vào đây:

- `homescreen.png` — Home, bàn chơi, danh hiệu
- `loginscreen2.png` — Màn đăng nhập

Nếu thiếu file, app dùng gradient đỏ (`BackgroundImage` fallback).

Kích thước gợi ý: **1080×1920** (portrait).
