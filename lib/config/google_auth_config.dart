/// OAuth Web Client ID (Firebase → Authentication → Google → Web SDK).
///
/// Dùng cho GIS trên web (meta tag + `clientId`) và `serverClientId` trên Android.
const String kGoogleWebClientId =
    '183989048778-l6bip2httunsi3qijlg8r9ovm0aq5ibp.apps.googleusercontent.com';
